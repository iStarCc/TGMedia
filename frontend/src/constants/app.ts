import versionInfo from "../../../version.json";

export const APP_VERSION = versionInfo.version;
export const VERSION_REPOSITORY = versionInfo.repository;
export const VERSION_BRANCH = versionInfo.branch;
